# Knowledge Garden 工作规则

## 唯一知识源

正式知识只维护在 `content/`。对话、候选稿、任务记录和迁移过程不作为知识正文保存。

## 沉淀触发

当用户表达“沉淀到知识库”“整理进知识库”“记录到知识库”“维护到知识花园”或语义相近的意图时，必须使用 `knowledge-distillation` Skill。

沉淀请求即授权在当前素材和主题范围内直接整理、修改 `content/`，无需先展示候选稿或再次确认写入。用户明确要求“仅出候选稿”“先给方案”或“不要修改文件”时，遵循该限制。

## 写入与 Review

写入前确定素材范围、文件操作、文章主类型和主题归属，处理重复内容与链接关系。素材或范围存在会实质影响结果的歧义时才询问用户，不为常规编辑选择重复请求授权。

写入并完成校验后，展示实际 diff，说明文件、主题关系、验证结果及必要的排除内容和不确定项，供用户 review。

用户提出修改意见后，在原任务范围内直接调整文档，重新校验并展示更新后的 diff，无需重新进入候选稿确认流程。

## 写入边界

- 写入前读取 `content/index.md` 和相关主题。
- 优先合并和重构已有主题，避免重复文章。
- 不创建失效内部链接，不写入敏感信息和未经确认的推测。
- 只修改当前任务涉及的知识文件和必要导航入口，保留无关的已有改动。
- 校验发现问题时直接修正任务范围内的内容；缺少必要事实或存在无法消解的知识冲突时，说明具体缺口，不用推测补齐。

## 视觉表达

- 当流程、层级、组件关系、状态变化或多对象对比用图解能明显降低理解成本时，可以加入表格、Mermaid 或其他合适的图示。
- 图解不是必备章节。单一事实、简单步骤或短列表用文字已经清楚时，不为了丰富形式而画图。
- 选择最小且足够的表达：对比优先表格，流程和状态变化优先流程图，层级和组成关系优先结构图。
- 图解应补充文字难以快速表达的关系，并配有必要说明；避免把相邻正文原样重复成图。
- 新增或调整排版形式、图示、表格、卡片等视觉内容时，须关注实际样式：简洁但不简陋，层次、留白和对齐清楚，与站点风格协调；优先复用已有样式，不为装饰增加复杂度。交付前检查实际渲染效果，具体检查项见 `.agents/skills/knowledge-distillation/references/quartz-writing.md` 的 `Style and Preview`。

## 校验

写入后依次运行：

1. `npm run check:content`
2. `npm run check`
3. `npm run quartz -- build`

完成后报告文件、关系和验证结果。

## Git 边界

知识沉淀和 review 修改只保留工作区改动，不执行 `git add`、`git commit` 或 `git push`。用户 review 后自行提交；沉淀请求及其确认均不构成 Git 操作授权。
