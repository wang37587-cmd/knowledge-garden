# Knowledge Garden 工作规则

## 唯一知识源

正式知识只维护在 `content/`。对话、候选稿、任务记录和迁移过程不作为知识正文保存。

## 沉淀触发

当用户表达“沉淀到知识库”“整理进知识库”“记录到知识库”“维护到知识花园”或语义相近的意图时，必须使用 `knowledge-distillation` Skill。

首次沉淀请求只能生成完整候选稿。即使同一请求包含“直接写入”“可以维护”等措辞，也不得修改文件。

## 候选稿和授权

候选稿必须说明素材范围、文件操作、文章类型、完整正文、链接调整、排除内容和不确定项。

只有用户在看到当前完整候选稿后明确回复“确认写入”“按这个版本保存”等指令，才能修改 `content/`。

候选稿发生任何调整后，旧授权失效，必须重新展示完整版本并重新确认。

## 写入边界

- 写入前读取 `content/index.md` 和相关主题。
- 优先合并和重构已有主题，避免重复文章。
- 不创建失效内部链接，不写入敏感信息和未经确认的推测。
- 只修改已确认的知识文件和必要导航入口。
- 校验发现需要实质改变正文时，重新进入候选稿阶段。

## 校验

写入后依次运行：

1. `npm run check:content`
2. `npm run check`
3. `npm run quartz -- build`

完成后报告文件、关系和验证结果。

## Git 边界

文件修改并完成校验后、执行任何提交前，必须向用户展示实际 diff 和验证结果供 review。除非用户随后明确要求提交，否则不得执行 `git commit`，也不得 push。
